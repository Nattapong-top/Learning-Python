'''📋 ทบทวนสั้นๆ (Cheat Sheet)
Append(x): เพิ่ม x ไปต่อ ท้ายสุด
Insert(index, x): แทรก x ลงใน ตำแหน่งที่ระบุ
Remove(x): ลบ ค่า x (ตัวแรกที่เจอ)
Pop(index): ดึงข้อมูลออก ตามตำแหน่ง (ถ้าไม่ใส่เลข จะดึงตัวสุดท้าย)

🎯 โจทย์ฝึกหัด 5 ข้อ
ลองเขียนโค้ดใส่กระดาษหรือ Editor ก่อน 
แล้วค่อยเลื่อนไปดูเฉลยด้านล่างนะครับ

ข้อที่ 1: การต่อแถว (Append)
มี List รายชื่อพนักงานที่เข้างานอยู่แล้วคือ 
staffs = ["Somchai", "Somsri"] 
โจทย์: มีพนักงานใหม่ชื่อ "Sombat" เดินมาต่อแถวเป็นคนสุดท้าย 
จงเขียนคำสั่งเพิ่ม Sombat เข้าไปใน List'''

staffs = ['Somchai', 'Somsri']

def add_staffs():
    staffs.append('Sombat')
    print(staffs)

add_staffs()

'''ข้อที่ 2: แทรกคิว VIP (Insert) จาก List เดิม 
(ข้อ 1) ตอนนี้คือ ["Somchai", "Somsri", "Sombat"] 
โจทย์: มีหัวหน้าชื่อ "Boss" ต้องการแทรกคิวมาอยู่ที่ ตำแหน่งแรกสุด (Index 0) 
จงเขียนคำสั่งแทรก Boss เข้าไป'''

def Insert_staff():
    staffs.insert(0, 'Boss')
    print(staffs)

Insert_staff()


'''ข้อที่ 3: ลบคนผิด (Remove) ตอนนี้ List คือ 
["Boss", "Somchai", "Somsri", "Sombat"] 
โจทย์: ปรากฏว่า "Somchai" ลาออก ให้เขียนคำสั่งลบชื่อ "Somchai" 
ออกจาก List โดยใช้ชื่อระบุโดยตรง'''

def remove_staffs():
    staffs.remove('Somchai')
    print(staffs)

remove_staffs()

'''ข้อที่ 4: ดึงใบล่าสุดออก (Pop) ตอนนี้ 
List คือ ["Boss", "Somsri", "Sombat"] 
โจทย์: เราต้องการดึงชื่อคน สุดท้าย ในแถวออกมาใช้งาน 
(และลบออกจาก List ไปด้วยในตัว) ให้เขียนคำสั่งนั้น'''

def pop_staff():
    end_staff = staffs.pop(-1)
    print(end_staff)

pop_staff()
    

'''ข้อที่ 5: ประยุกต์รวม (Combo)
มี List ว่างเปล่าอยู่ 1 ตัว: tasks = [] 
โจทย์: จงเขียนคำสั่งตามลำดับต่อไปนี้:
เพิ่ม "Coding" ต่อท้าย
เพิ่ม "Meeting" ต่อท้าย
แทรก "Coffee" ไว้หน้าสุด
ลบ "Meeting" ทิ้ง'''

def list_method_combo():
    tasks = []
    tasks.append('Coding')
    print(tasks)
    tasks.append('Meeting')
    print(tasks)  
    tasks.insert(0, 'Coffee')
    print(tasks)
    tasks.remove('Meeting')
    print(tasks)
list_method_combo()
print('============ End Function ============\n')
'''🛠️ โจทย์ชุดลงมือทำ: A I R P ในชีวิตจริง
ข้อที่ 1: ระบบบัตรคิว (Queue)
สถานการณ์: ป๋ากำลังทำระบบรับเรื่องแจ้งซ่อมคอมพิวเตอร์ ใครมาก่อนต้องได้ซ่อมก่อน (First In, First Out)
สร้าง List ว่างๆ ชื่อ repair_queue
มีคนแจ้งซ่อมเข้ามา 3 คน ให้ เพิ่ม ชื่อ "User A", "User B", "User C" เข้าไปในคิวตามลำดับ
ช่างว่างแล้ว! ให้ ดึง คนแรกสุด ("User A") ออกไปซ่อม และเก็บชื่อคนที่ถูกดึงไว้ในตัวแปรชื่อ current_job
สั่งพิมพ์ current_job และ repair_queue ที่เหลือออกมาดู'''
import random
import time
def queue_first_in_first_out():

    repair_queue = []

    queue_name = ['User A', 'User B', 'User C']
    for i in queue_name:
        repair_queue.append(i)
        print(i)
        # time.sleep(1)

    print('คิวทั้งหมด', len(repair_queue), repair_queue)

    while repair_queue:
        technician_free = random.choice([True,False])
        print('ช่างว่าง',technician_free)

        if technician_free:
            current_job = repair_queue.pop(0)
            technician_free = False
            print('กำลังซ่อม', current_job)
            print('คิวที่เหลือ', len(repair_queue), repair_queue)
            
        else:
            print('ช่างไม่ว่าง กรุณารอสักครู่')
            print('คิวที่เหลือ',len(repair_queue), repair_queue)

queue_first_in_first_out()
print('============ End Function ============\n')


'''ข้อที่ 2: งานด่วนแทรกเข้ามา (Insert)
สถานการณ์: ป๋ามีรายการสิ่งที่ต้องทำ (To-do list) อยู่แล้ว แต่จู่ๆ หัวหน้าสั่งงานด่วนที่สุดเข้ามา
มี List เดิมคือ todo = ["เช็คอีเมล", "เขียนรายงาน", "กินข้าวเที่ยง"]
หัวหน้าสั่งด่วน! ให้ แทรก งาน "ซ่อม Server" ไปไว้ อันดับแรกสุด (index 0)
เพื่อนร่วมงานฝากงาน ให้ แทรก งาน "ซื้อกาแฟ" ไปไว้ หน้า การกินข้าวเที่ยง 
(กินข้าวเที่ยงเดิมอยู่ index 2 แต่หลังจากแทรกงานแรก index อาจจะเปลี่ยน ลองนับดูดีๆ นะครับ)
พิมพ์ todo ออกมาดู'''
def Insert_list_todo():
    todo = ["เช็คอีเมล", "เขียนรายงาน", "กินข้าวเที่ยง"]

    while True:
        print('1. show todo')
        print('2. Insert todo')
        print('q ออก')
        
        acction = input('พิมพ์ 1 หรือ 2 q ออก: ')
        
        if acction == 'q':
            print('ออก')
            break
        
        elif acction == '1':
            print(todo)
        
        elif acction == '2':
            print('Insert todo')
            num_index, intodo = input('index, เรื่อง: ').split(',')
            todo.insert(int(num_index), intodo.strip())
            print(todo)

# Insert_list_todo()
print('============ End Function ============\n')

'''ข้อที่ 3: ลบโปรแกรมที่ไม่ใช้ (Remove)
สถานการณ์: ป๋าต้องการเคลียร์รายชื่อโปรแกรมในเครื่อง
มี List programs = ["Chrome", "Word", "Virus.exe", "Excel"]
จงเขียนคำสั่ง ลบ "Virus.exe" ออกจาก List โดยระบุชื่อมันลงไปตรงๆ
พิมพ์ programs ออกมาเพื่อยืนยันว่าไวรัสหายไปแล้ว'''
def remove_list_programs():
    programs = ["Chrome", "Word", "Virus.exe", "Excel"]

    while True:
        print('1. ลบโปรแกรม')
        print('any ออกจากโปรแกรม')

        acction = input('1 or q: ')
        if acction == '1':
            print(programs)
            remove_program = input('พิมพ์ชื่อโปรแกรม: ')
            if remove_program in programs:
                programs.remove(remove_program)
                print('ลบสำเร็จแล้ว')
                print(programs)
            else:
                print(f'ไม่พบโปรแกรม {remove_program} ในระบบ')
        else:
            print('ออกจากโปรแกรม')
            break

# remove_list_programs()
print('============ End Function ============\n')

'''ข้อที่ 4: ปุ่ม Undo (Stack)
สถานการณ์: ป๋ากำลังพิมพ์เอกสาร แล้วเผลอพิมพ์ผิด ต้องการกดย้อนกลับ (Undo) 
ซึ่งต้องเอาตัวล่าสุดออกก่อน (Last In, First Out)
สร้าง List actions = ["พิมพ์ ก.", "พิมพ์ ข.", "พิมพ์ ค.", "พิมพ์ ง."]
ผู้ใช้กด Undo 1 ครั้ง: ให้ ดึง ตัวสุดท้าย ("พิมพ์ ง.") ออกจาก List
ผู้ใช้กด Undo อีก 1 ครั้ง: ให้ ดึง ตัวสุดท้าย (ตอนนี้คือ "พิมพ์ ค.") ออกอีก
พิมพ์ actions ที่เหลือออกมา'''

def pop_list_undo():

    action = []

    fake_actions = ["พิมพ์ ก.", "พิมพ์ ข.", "พิมพ์ ค.", "พิมพ์ ง."]
    for i in fake_actions:
        action.append(i)
        print('append:', i)
        time.sleep(1)

    undo_num = 2
    for i in range(undo_num):
        undo = action.pop()
        print('undo:', undo)
        time.sleep(1)

    print(action)

pop_list_undo()


