🧠 ตัวแปรประเภท Input — ชื่อมาตรฐานที่ Dev ใช้
1️⃣ Input / Input Data

ความหมาย: ข้อมูลที่รับเข้ามา
รูปแบบ: parameter, argument, raw data

ตัวอย่างชื่อ:

input_data

user_input

raw_input

📌 ใช้เมื่อ:

ข้อมูลยัง “ดิบ”

ยังไม่ validate

2️⃣ Parameter ⭐

ความหมาย: ตัวแปรรับค่าที่ประกาศในฟังก์ชัน

def func(x, y):   # x, y = parameters


📌 คำนี้ใช้บ่อยมากในเอกสาร / interview

3️⃣ Argument

ความหมาย: ค่าที่ส่งเข้าไปตอนเรียกฟังก์ชัน

func(10, 20)   # 10, 20 = arguments


📌 Parameter ≠ Argument (แต่หลายคนใช้ปน)

4️⃣ Raw Input

ความหมาย: input ที่ยังไม่ผ่านการตรวจ

ตัวอย่าง:

string จาก input()

list ที่ยังไม่กรอง

ตัวอย่างชื่อ:

raw_line

raw_values

📌 Dev จะใช้คำว่า raw เพื่อเตือนตัวเองว่า

“ห้ามใช้ตรง ๆ”

5️⃣ Validated Input

ความหมาย: input ที่ผ่านการตรวจแล้ว
พร้อมใช้งานจริง

ตัวอย่างชื่อ:

valid_numbers

clean_data

📌 raw → validated = workflow มาตรฐาน

6️⃣ Payload

ความหมาย: input ที่ส่งเข้ามาเป็นชุด
(ใช้บ่อยใน API / system)

ตัวอย่างชื่อ:

payload

request_data

📌 อันนี้จะเจอเยอะตอนขึ้น Phase ระบบ

🧾 สรุปให้จำง่าย (Input Cheat Sheet)
ชื่อ (EN)	            ใช้ตอนไหน	        ความหมาย
input / input_data	    ทั่วไป	            ข้อมูลที่รับมา
parameter	            ใน def	          ตัวแปรรับค่า
argument	            ตอนเรียก	        ค่าที่ส่งเข้า
raw_input	            ยังไม่เช็ค	          ข้อมูลดิบ
validated_input	        เช็คแล้ว	         ใช้ได้จริง
payload	                เป็นชุด	            ข้อมูลจากระบบ


🎯 วิธีตั้งชื่อแบบ Dev (สูตรสั้นมาก)

ถ้ายังไม่เช็ค → ใส่คำว่า raw
เช็คแล้ว → ใส่ valid / clean

ตัวอย่าง:

raw_list
valid_numbers

🔗 เชื่อมกับโค้ดของป๋า
def max_from_valid_input(nums_list):

ชื่อนี้ดีมาก เพราะสื่อว่า:

input = nums_list

แต่ใน function → มี validation

ถ้าจะชัดขั้นสุด:

raw_list → ก่อน loop

max_value → ผลลัพธ์