🧭 เลือกใช้ .isdigit() vs isinstance() (พร้อมกรณี float)
1️⃣ isinstance(n, int) — ใช้เมื่อไร
ใช้เมื่อ

input spec ชัด: รับ จำนวนเต็มเท่านั้น

ข้อมูลมาจาก

function call

API

database

ระบบภายใน

ตัวอย่างสถานการณ์

utility function

business logic

production backend

ข้อดี

เร็ว

ชัด

บอก intent ตรง

if isinstance(n, int):
    ...

ข้อจำกัด

ไม่รับ 5.0

ไม่รับ "5"

2️⃣ str(n).lstrip('-').isdigit() — ใช้เมื่อไร
ใช้เมื่อ

input ไม่เชื่อถือ

รับค่าจาก

user input

file

log

external source

ตัวอย่าง

script

CLI

data cleaning

ETL

ข้อดี

กัน input เพี้ยนได้ดี

รับ "5", "-12"

if str(n).lstrip('-').isdigit():
    num = int(n)

ข้อจำกัด

"5.0" ❌

"3e2" ❌

3️⃣ แล้วถ้าต้อง “รับ float” ล่ะ?

ต้องถามก่อน:

float แบบไหน

🔹 กรณี A: รับเฉพาะ float ที่ “เป็นจำนวนเต็ม”

เช่น 5.0, -12.0

if isinstance(n, (int, float)) and n.is_integer():
    num = abs(int(n))


✔️ ใช้บ่อยมากในงานจริง
✔️ ปลอดภัย
✔️ ชัด

🔹 กรณี B: รับ float ทุกค่า (เช่น 12.34)

❗ ต้อง “นิยามใหม่” ว่า เลขโดดคืออะไร

ตัวเลือกที่ใช้จริง:

แนวทาง 1: ตัดจุดทศนิยมทิ้ง
num = int(abs(n))

แนวทาง 2: แปลงเป็น string แล้วตัด .
for ch in str(n):
    if ch.isdigit():
        ...


📌 ต้องเลือก อย่างใดอย่างหนึ่ง
ไม่มีคำตอบตายตัว

4️⃣ ตารางตัดสินใจ (สำคัญมาก)
สถานการณ์	ควรใช้
ระบบภายใน	isinstance
รับ input จากคน	isdigit
Data cleaning	isdigit
Production backend	isinstance
รองรับ float = int	is_integer()
float ทั่วไป	ต้องนิยามใหม่
5️⃣ กฎทองของมืออาชีพ

อย่าเขียนโค้ดเพื่อเดาว่า input คืออะไร
เขียนโค้ดตาม contract ที่ตกลงไว้

ถ้า spec บอก int → reject float

ถ้ารับ float → ระบุชัดว่าทำยังไงกับจุดทศนิยม

🎯 สรุปจำสั้น

isinstance = เชื่อ input

isdigit = ไม่เชื่อ input

float = ต้องนิยามก่อนเสมอ