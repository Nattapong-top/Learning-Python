✅ logic-template.md

ใช้ซ้ำทุกโจทย์
เนื้อหาแค่:

Input: 

Output: 

Logic steps: 

Edge cases: 

Order of checks: 

👉 ไฟล์เดียว ช่วยให้ “คิดออกโดยไม่ต้องเขียนโค้ด”

Problem: Find max duplicate count
Input: list of values
Output: integer (max count)
Logic Steps:
- Loop each value
- Count same values
- Track max
Edge Cases:
- empty list
- non-int
Order:
- validate
- count
- compare
