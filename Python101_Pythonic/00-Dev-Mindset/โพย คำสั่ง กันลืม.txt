📜 โพยกันลืม(Python Cheat Sheet) ฉบับป๋าณัฐพงศ์
1. การรับข้อมูล (Input) และตัวเลข
รับค่าเป็นข้อความ: name = input("ชื่ออะไร: ")

รับค่าแล้วแปลงเป็นเลขจำนวนเต็ม: num = int(input("กี่แก้ว: "))

เช็กว่าเป็นตัวเลขไหม (กันโปรแกรมพัง):

Python

if text.isdigit():
    # ทำงานต่อถ้าเป็นตัวเลข
2. การแสดงผล (Output) แบบมือโปร
ใช้ f-string (เอาตัวแปรใส่ในข้อความ): print(f"ราคารวม {total} บาท")

ขีดเส้นคั้นสวยๆ: print("-" * 20)

โชว์ข้อความแบบมีเงื่อนไข (Inline If): print(f"เงินทอน: {'ไม่มี' if change == 0 else change}")

3. การวนลูปและการตัดสินใจ (Control Flow)
วนลูปจนกว่าจะสั่งหยุด:

Python

while True:
    ...
    if order == 'end':
        break # ออกจากลูป
เช็กเงื่อนไขหลายชั้น: if ... elif ... else

4. การจัดการไฟล์ (File I/O)
บันทึกต่อท้ายไฟล์ (Append):

Python

with open('sales.txt', 'a', encoding='utf-8') as file:
    file.write("ข้อความที่อยากบันทึก\n")
5. ฟังก์ชัน (Function) - "สร้างลูกน้องมาช่วยงาน"
โครงสร้าง:

Python

def ชื่อฟังก์ชัน(ตัวแปรที่รับมา):
    # งานที่ให้ทำ
    return ผลลัพธ์ # ส่งงานคืน
6. วันเวลา (Datetime)
ดึงเวลาปัจจุบัน:

Python

from datetime import datetime
now = datetime.now()
timestamp = now.strftime('%d/%m/%y %H:%M:%S')